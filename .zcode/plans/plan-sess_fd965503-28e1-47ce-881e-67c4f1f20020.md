# Лобі кімнати — нова сторінка за макетом + лагодження join-флоу

План уже узгоджено (усі 4 рішення — «рекомендовано»), ти підтвердив старт. Обліковий запис uhonogoruk@gmail.com використаю для логіну та створення тестової кімнати на етапі браузерної перевірки.

## mafia-server
1. `MyRoomState.ts`: поле `@type("number") maxPlayers = 12`.
2. `MafiaRoom.ts`: видалити зламаний `joinAsPlayer`; додати `setMaxPlayers` (тільки хост, валідація 6..12 і ≥ поточних гравців → `maxClients` + `state.maxPlayers`); `onJoin` призначає `seatIndex = players.size`; перевірка хоста через `isHost`.

## mafia-app — join-флоу
3. `create-game.tsx`: `client.create('mafia_room', { token, guestId: user.sub })` + `localStorage: room_${roomId}_hostClaim = user.sub`.
4. `identity.ts`: `createGuestIdentity(roomId, name, presetGuestId?)`.
5. `join-form.tsx`: читає hostClaim → хост отримує guestId = user.sub.
6. Новий `room-context.tsx`: `RoomContext = createRoomContext()` + `RoomConnectionProvider` — підключення лише за наявності token+identity (`joinById(roomId, { token, name, guestId })`), банер серверних `"error"` повідомлень.
7. Видалити `room-lobby.tsx`, `player-list.tsx`.

## mafia-app — сторінка за макетом
8. Route groups: кореневий layout без шапки; `(main)/layout.tsx` з AppNav/AppLayout/AppFooter; туди переносяться `page.tsx`, `dashboard/`, `(auth)/`. URL-и не змінюються.
9. `room/[roomId]/page.tsx`: контейнер `max-w-[1600px]`, `RoomTopBar`, `.room-body-grid` (нова класа: `grid gap-4 lg:grid-cols-[1fr_20rem]`), сітка + aside.
10. `room-top-bar.tsx`: фаза (мапа `src/lib/phase.ts`), «Гравці: N/M» (maxPlayers зі стану), плейсхолдер-іконки Mic/Camera/Settings.
11. `player-grid.tsx`: `maxPlayers` слотів (grid-cols-4), гравці за `seatIndex`, аватар-літера + ім'я + корона хоста, порожні — `bg-muted aspect-square`.
12. `room-sidebar.tsx` («Кімната:»): рестилізований `invite-link.tsx`, сегмент 10/11/12 (хост → `setMaxPlayers`), склад ролей (`src/lib/role-composition.ts`), «Перемішати» → `shuffle_players`, «Ще N гравців»/«Почати гру» → `startGame`.
13. `rules-card.tsx` + `rules-modal.tsx`: 4 підказки + модалка з повними правилами (`src/lib/rules.ts`, чернетка українською).
14. `globals.css`: +`.room-body-grid`, −`.room-layout-grid`. Нові тексти українською, кольори через токени.

## Перевірка
1. `npm run build` + `lint` (app), `tsc --noEmit` (server).
2. Node e2e-скрипт (tsx + @colyseus/sdk): хост+гость у стані, isHost/seatIndex, setMaxPlayers 10 → 11-й не входить, shuffle, startGame.
3. Браузер: логін під наданим акаунтом → створю кімнату сам → перевірю хостський вигляд; гостом приєднаюсь за згенерованим посиланням у другій вкладці.
4. Кімнату для тестів створюю заново після впровадження (старі не мають хоста).

Обмеження: іконки мікрофона/камери/налаштувань — плейсхолдери; assignRoles досі заглушка (кнопка «Почати гру» шле повідомлення, ігрового циклу ще нема).
